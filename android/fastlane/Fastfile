default_platform(:android)

platform :android do

  desc "Deploy a beta version to the Google Play"
  lane :beta do
    begin
      gradle(task: "clean bundleRelease")
      upload_to_play_store(track: 'beta')
    rescue => e
      UI.user_error!("Failed to deploy beta version: #{e.message}")
    end
  end

end
