import 'package:flutter/material.dart';

class SportsGymnasticPage extends StatelessWidget {
  const SportsGymnasticPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sports Gymnastic Page'),
      ),
    );
  }
}
